<div id="favorite-<%= @app.id %>">
  <% if !current_user.nil? && current_user.favorite?(@app) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>